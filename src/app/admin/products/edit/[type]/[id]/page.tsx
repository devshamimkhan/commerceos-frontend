import { notFound } from 'next/navigation'; import { ProductEditor } from '@/components/products/product-module'; import { getCatalogServer } from '@/lib/catalog-server'; import { getProductServer, getProductSelectorServer } from '@/lib/product-server';
export default async function Page({params}:{params:Promise<{type:string,id:string}>}){const {type,id}=await params;if(!['simple','variable','combo'].includes(type))notFound();const [product,categories,tags,brands,attributes,products]=await Promise.all([getProductServer(id),getCatalogServer('categories'),getCatalogServer('tags'),getCatalogServer('brands'),getCatalogServer('attributes'),type==='combo'?getProductSelectorServer():Promise.resolve({success:true,data:[]})]);if(!product.success||product.data.type!==type)notFound();return <ProductEditor type={type} existing={product.data} catalogs={{categories:categories.success?categories.data:[],tags:tags.success?tags.data:[],brands:brands.success?brands.data:[],attributes:attributes.success?attributes.data:[]}} comboOptions={products.success?products.data:[]}/>}
